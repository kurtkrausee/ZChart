// Re-export from the canonical location to avoid import conflicts
export type { ChartConfig, DeepPartial } from '../../core/ChartOptions';
export { defaultOptions, mergeOptions } from '../../core/ChartOptions';
